const celsiusToFahrenheit = (value) => (value * 1.8) + 32;
const fahrenheitToCelsius = (value) => (value - 32) / 1.8;

const abc = 'ABC';
const Ewerton = {
  name: 'Ewerton',
  idade: 25,
}

module.exports = {
  celsiusToFahrenheit,
  fahrenheitToCelsius,
  abc,
  Ewerton,
};